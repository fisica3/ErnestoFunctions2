# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- none

resources:
  pipelines:
  - pipeline: functionsorigin   # Name of the pipeline resource
    source: ErnestoFunctions # Name of the triggering pipeline

variables:
  # Azure Resource Manager connection created during pipeline creation
  azureSubscription: 'Sponsorship for Functions'

  # Function app name
  functionAppNameDev: 'democnp03'
  functionAppNameUat: 'demofunctions04'
  # Agent VM image name
  vmImageName: 'windows-2019'

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: DeployUAT
  displayName: Deploy UAT
  jobs:
  - deployment: JDeployUat
    displayName: Job Deploy Uat
    environment: 'envuat'
    pool:
      vmImage: $(vmImageName)
    strategy:
      runOnce:
        deploy:
          steps:
          - script: ls $(Pipeline.Workspace)/functionsorigin/
          #- task: AzureFunctionApp@1
          #  displayName: 'Azure functions app deploy'
          #  inputs:
          #    azureSubscription: '$(azureSubscription)'
          #    appType: 'functionApp'
          #    appName: '$(functionAppNameUat)'
          #    package: '$(Pipeline.Workspace)/functionsorigin/drop/$(Build.BuildId).zip'
          #    deploymentMethod: 'zipDeploy'              
